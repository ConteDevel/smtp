#ifndef __UTILS_H__
#define __UTILS_H__

#include <libs.h>   //!< standart libs
#include <log.h>    //!< log libs
#include <sl.h>     //!< sl_t struct

//#define MIN(a, b) ((a > b) ? (b) : (a))

#endif // __UTILS_H__